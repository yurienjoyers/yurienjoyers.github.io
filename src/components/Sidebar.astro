---
interface SidebarSection {
    pageHref: string;
    sectionLabel: string;
}

interface SidebarGroup {
    groupLabel: string;
    sections: SidebarSection[];
}

interface Props {
    groups?: SidebarGroup[];
    activePage?: string;
}

const { groups = [], activePage = "" } = Astro.props as Props;
---

<aside class="sidebar" aria-label="Blog sidebar">
    <a class="site-title" href="/">
        <img
            class="site-icon"
            src="/assets/logo.png"
            alt=""
            width="22"
            height="22"
            transition:persist
        />
        <span>yurienjoyers</span>
    </a>

    {
        groups.map((group) => (
            <section class="group" aria-label={`${group.groupLabel} posts`}>
                <p class="group-label">{group.groupLabel}</p>
                <ul>
                    {group.sections.map((section) => (
                        <li>
                            <a
                                class:list={[
                                    "sidebar-link",
                                    { active: section.pageHref === activePage },
                                ]}
                                href={section.pageHref}
                            >
                                {section.sectionLabel}
                            </a>
                        </li>
                    ))}
                </ul>
            </section>
        ))
    }
</aside>
